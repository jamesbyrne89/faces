import React, { Component } from 'react';
import NewPhoto from '../views/NewPhoto';




const LocationsDropdown = (props) => {
    let isVisible = false;
    return ( <div className="menu-dropdown {isVisible ? }">Dummy Content</div>
    );
}

export default LocationsDropdown;